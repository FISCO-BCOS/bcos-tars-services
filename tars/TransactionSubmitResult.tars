#include "Common.tars"

module bcostars {
    struct TransactionSubmitResult {
        1 optional int status;
        3 optional vector<byte> txHash;
        5 optional vector<byte> blockHash;
        7 optional long transactionIndex;
        8 optional string nonce;
    };
};